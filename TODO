* more flexible date ranges, e.g. a `days` argument that can be specified instead of an end date, etc. (code can mostly be copied from `python-google-analytics`)
* README
* unit tests
* the entire CLI (authentication, page posts and post insight queries)